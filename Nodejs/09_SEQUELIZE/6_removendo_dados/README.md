# Nessa etapa são filtrados dados utilizando o WHERE.

# Para remover itens utilizando o método destroy. 

# É parecido com a função de resgatar usuário, só que será um POST.

# Precisamos também criar um formulário no frontend para executar a ação.