# Nessa etapa veremos como deletar apenas os dados de uma tabela que é relacionada a outra.

# Apesar de ser um dado relacionado, podemos remover os dados da tabela de forma independente, usando o destroy() com where. 