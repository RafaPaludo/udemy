# Editando dados

- O primeiro passa da edição nós já sabemos: *resgatar dado e colocar no formulário em uma nova view*;
- Para isso vamos precisar de uma *função no Controller*, pegando os dados via *Model*;
- E então criar a *rota que corresponde a um id de uma Task*, neste caso;
- *Preenchendo os valores os inputs* com o que foi resgatado do banco;